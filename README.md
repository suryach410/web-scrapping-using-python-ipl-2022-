# web-scrapping-using-python-ipl-2022-
Web scraping using Python helps extract IPL auction data from the official website and organize it in a structured table format using **pandas**. It involves sending a request to the auction page, parsing the HTML with **BeautifulSoup**, . Finally, the cleaned data can be saved as a **CSV file** for further analysis or visualization.
